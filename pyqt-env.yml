name: pyqt-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pyqt
  - matplotlib
  - pip
  - pip:
      - pyqt5-tools # Optional: for Qt Designer and other toolsname: pyqt-env
